{% extends "layouts/transport.njk" %}


{% block content %}
  <div class="container">
    <section class="section">
    <!-- Main container -->
    <nav class="level">
      <!-- Left side -->
      <div class="level-left">
        <div class="level-item">
          <p class="mb-6 title is-3">司机列表</p>
        </div>
      </div>
      <!-- Right side -->
      <div class="level-right">
        <p class="level-item">
          <a href="/dashboard/create/member" class="button is-info">New</a>
        </p>
      </div>
    </nav>
      <div class="table-container">
        <table class="table is-fullwidth is-hoverable ">
          <thead>
            <tr>
              <th>司机编号</th>
              <th>司机名</th>
            </tr>
          </thead>
          <tbody>
            {% for result in results %}
              <tr>
                <th>
                  <a href="/dashboard/member/{{ result.driverId }}">
                    <span class="icon">
                      <i class="fas fa-book" aria-hidden="true"></i>
                    </span>
                    <span>#{{ result.driverId }}</span>
                  </a>
                </th>
                <td>
                  <span class="has-text-grey-dark">{{ result.driverName }}</span>
                </td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
      </section>
    </div>
  {% endblock %}
